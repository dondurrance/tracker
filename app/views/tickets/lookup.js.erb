
var data =  <%=h sanitize(@rooms.order(:name).to_json(:only => [:name, :id]))%>;

var location_id = <%=h @location.id %>;

var hidden = document.getElementsByTagName('hidden')[1];

var select = document.getElementsByTagName('select')[1];
select.options.length = 0; // clear out existing items
for(var i=0; i < data.length; i++) {
    var d = data[i];
    select.options.add(new Option(d.name, d.id))
}

$('hidden_fields').update('<input type="hidden" value="<%=h @location.id %>" name="ticket[location_id]" id="ticket_location_id">')

